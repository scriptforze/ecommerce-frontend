import { SideBar, Layout, NavBar } from "@/modules/common/components";

const AdminLayout = () => {
  return (
    <Layout>
      <NavBar />
      <SideBar />
    </Layout>
  );
};

export default AdminLayout;

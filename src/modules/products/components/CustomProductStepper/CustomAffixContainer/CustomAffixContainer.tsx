import { StyledAffixContainer } from "./styled";
import { CustomAffixContainerProps } from "./types";

export const CustomAffixContainer = (props: CustomAffixContainerProps) => {
  return <StyledAffixContainer {...props} />;
};
